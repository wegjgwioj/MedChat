# Python runtime (classroom demo)
fastapi==0.125.0
uvicorn==0.38.0
pydantic==2.12.5

# LLM + agent orchestration (Python 3.9 compatible)
openai==0.28.1
langchain==0.0.352
langchain-community==0.0.20
langgraph==0.0.20

# Local RAG (Faiss-HNSW + embeddings)
faiss-cpu==1.13.2
opensearch-py==2.8.0
sentence-transformers==2.6.1
transformers==4.36.2
BCEmbedding>=0.1.5  # Required for embedding & reranking (maidalun1020/bce-embedding-base_v1)

# PyTorch - GPU acceleration for embeddings
# Supports: CUDA (NVIDIA), MPS (Apple Silicon), CPU
# Default pip install gets CPU+MPS version (~2GB)
# For CUDA, install separately: pip install torch --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0

# Utilities
requests
httpx
python-multipart
wikipedia
tiktoken
typing_extensions
redis==5.2.1

# Dev / tests
pytest
