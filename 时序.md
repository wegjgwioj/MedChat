```mermaid
sequenceDiagram
  autonumber
  participant C as Client (前端)
  participant API as FastAPI Router (app/agent/router.py)
  participant LG as LangGraph Orchestrator (app/agent/graph.py)
  participant DB as SQLite Session Store (app/agent/storage_sqlite.py)
  participant RAG as RAG Core (app/rag/rag_core.py)
  participant LLM as LLM Service (DeepSeek/LangChain)

  C->>API: POST /v1/agent/chat_v2 {session_id, user_message}
  API->>LG: 调用 run_chat_v2_turn()
  
  Note over LG: 进入状态机节点流转
  
  LG->>DB: load_session(session_id) 加载历史状态
  DB-->>LG: 返回 AgentSessionState (messages, slots)

  rect rgb(240, 240, 240)
    Note right of LG: 节点 1: SafetyGate (安全门)
    LG->>LG: 检查红旗症状 (_looks_like_red_flag)
    alt 命中高风险关键词
        LG->>LG: mode = "escalate", 填充急诊模板
        Note over LG: 跳过后续业务节点，直接进入持久化
    end
  end

  rect rgb(240, 240, 255)
    Note right of LG: 节点 2: MemoryUpdate (记忆更新)
    LG->>LG: 追加消息并更新摘要 (summary)
    LG-->>LLM: (可选) 抽取/合并槽位信息 (slots)
  end

  rect rgb(240, 255, 240)
    Note right of LG: 节点 3: TriagePlanner (分诊规划)
    LG->>LG: 判断 slots 是否足以回答 (_slots_sufficient_for_answer)
    alt 信息不足 (mode = "ask")
        LG->>LG: 构造结构化追问 questions
    else 信息充足 (mode = "answer")
        Note over LG: 流程继续进入 RAG 检索
        LG->>RAG: retrieve(rag_query, top_k, top_n)
        RAG-->>LG: 返回证据列表 (evidence)
        
        LG->>LLM: compose_answer(user_msg, evidence)
        LLM-->>LG: 返回带引用的回答 + 免责声明
    end
  end

  LG->>DB: save_session() 持久化最新状态
  LG-->>API: 返回执行结果 (mode, answer, trace)
  API-->>C: JSON Response
